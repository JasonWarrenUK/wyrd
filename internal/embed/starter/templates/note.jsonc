{
	// Note template — defines fields for reference notes.
	// title is now a core node field, not a template field.
	"type_name": "note",
	"fields": {
		"tags": {
			"type": "string_array",
			"optional": true,
			"description": "Tags for categorisation"
		}
	}
}
